import React from 'react';
import styles from './all.module.css';

const Settings = (props) => {
    return (
        <div className={styles.s}>
            Setting - NASTROY BRATUHA
        </div>
    )
}

export default Settings;

